@* <--!Strong View--> *@
@model List<Trainee>

<div class="card-container">
    @foreach (var Trainier in Model)
    {
        <div class="card" style="width: 18rem;">
            <img src="/Img/@Trainier.Img" class="card-img-top" alt="...">
            <div class="card-body">
                <h5 class="card-title">@Trainier.Name</h5>
                <a href="/Trainee/details/@Trainier.Id" class="btn btn-primary">View Details</a>
            </div>
        </div>
    }
</div>

<style>
    .card-container {
        display: flex;
        flex-wrap: wrap;
        gap: 1rem;
    }

    .card {
        flex: 1 1 18rem;
    }
</style>
